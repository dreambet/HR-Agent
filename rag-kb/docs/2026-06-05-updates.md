# 2026-06-05 更新

## 打招呼"假成功"问题完整修复

### 问题
greet_candidate.py 脚本报告成功，但智联招聘消息列表中实际没有发出消息。

### 根因
1. "使用并发送"按钮使用 JavaScript `btn.click()`，智联招聘KMUI框架不响应
2. 切换到"自己设置招呼语"后，模态框遮挡层阻止 textarea 填写

### 修复
- 所有关键按钮改用 Playwright `click(force=True)` + 坐标回退
- 跳过"自己设置招呼语"，直接使用系统默认AI招呼语
- 新增 Toast 监控、网络请求监控、对话框关闭验证

### 验证
- 蒋先生打招呼测试成功 ✅
- 许先生确认已有聊天记录 ✅

### 规则
智联招聘页面上所有按钮点击都应使用 Playwright click，不使用 JS click。
