# 2026-05-25 知识库更新

## 核心功能优化记录

### 1. 详情报告获取增强（get_resume_detail.py）

**问题**：详情脚本依赖浏览器页面结果，候选人不在当前页时无法获取

**解决方案**：
- 读取 /tmp/zhaopin_search_context.json 获取候选人 resume_number
- 用与初筛一致的搜索API（pageSize=50、TIME排序）查找原始项
- 直接调用 POST /api/resume/detail 获取详情
- 安全校验：按 resumeNumber 精确匹配 + 姓名校验

**验证**：丁女士、雷女士、申先生均成功获取

### 2. 打招呼功能路由修复（greet_candidate.py）

**问题**：路由拦截 `**/api/talent/search/list**`** 未生效，实际URL带query参数

**解决方案**：
- route pattern 改为 `**/api/talent/search/list**`（尾部加**）
- 注入与初筛一致的过滤参数：filteringChatted=false、filteringRead=false、filteringDownloaded=false、sort=TIME、pageSize=50

**验证**：申先生打招呼成功

### 3. 城市映射增强解析（search_resumes.py / greet_candidate.py / get_resume_detail.py）

**问题**：get_city_id 精确匹配，"深圳-宝安"无法匹配，fallback到周口（734）

**解决方案**：
```python
def get_city_id(city_name):
    # 优先精确匹配
    if city_name in city_map:
        return city_map[city_name]
    # 支持 "市-区" / "省-市" 格式自动提取城市
    for sep in ['-', ' ', '　']:
        if sep in city_name:
            city_part = city_name.split(sep)[0]
            if city_part in city_map:
                return city_map[city_part]
```

**已支持城市**：
- 深圳-宝安→765（修复前：fallback到734周口）
- 广东-东莞→779
- 东莞-清溪→779
- 广东-惠州→773（新增）

**其他修复**：
- greet_candidate.py：郑州762→701

### 4. 打招呼脚本新增 --index 参数

**功能**：
- 支持 `--index N` 按初筛报告序号定向联系
- 增强同名候选人去重逻辑（年龄+工作年限+学历多字段匹配）

**使用方式**：
```bash
python3 greet_candidate.py --name "李先生" --index 4 --keyword "java开发工程师" --location "深圳-宝安"
```

## 今日教训

1. **序号指定直接执行**：用户说"获取序号4李先生"，应直接按序号取 resumeNumber，不做姓名二次确认
2. **上下文污染**：新旧搜索结果混用同一上下文文件，需注意搜索条件的连续性
3. **城市映射 fallback**：带区级的城市名需要分层解析

## 待优化项（暂不执行）

1. **职位匹配确认**：自动匹配职位与目标岗位不一致时，先让用户确认再发送
2. **飞书群报告可点击下载**：初筛/详细报告在飞书群内可点击查看

---

*本文件由系统自动生成，用于 RAG 知识库索引。*
